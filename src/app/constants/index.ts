export const CELL_STATUSES = Object.freeze({
  revealed: 'revealed',
  hidden: 'hidden',
});

export const CELL_TYPES = Object.freeze({
  bomb: 'bomb',
  flag: 'flag',
  number: 'number',
  space: 'space',
  unknown: 'unknown',
});
